/**import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AnaliseDeCadastro } from './analise-de-cadastro';

describe('AnaliseDeCadastro', () => {
  let component: AnaliseDeCadastro;
  let fixture: ComponentFixture<AnaliseDeCadastro>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AnaliseDeCadastro]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AnaliseDeCadastro);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
